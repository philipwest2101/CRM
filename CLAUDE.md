# Project Guidelines

## Git Workflow

- Always develop on the designated feature branch first.
- Resolve all merge conflicts on the feature branch before touching the default branch.
- Only fast-forward (clean) merges go into the default branch — no direct commits on it.
- Never push to a different branch without explicit permission.
